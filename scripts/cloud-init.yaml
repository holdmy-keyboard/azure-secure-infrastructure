#cloud-config

package_update: true

packages:
  - nginx

write_files:
  - path: /var/www/html/index.html
    owner: root:root
    permissions: '0644'
    encoding: b64
    content: ${webpage_content}

runcmd:
  - systemctl enable nginx
  - systemctl restart nginx
